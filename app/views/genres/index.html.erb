<h1>ジャンル一覧</h1>

<table class="table">
	<tbody>
		<% @genres.each do |genre| %>
			<tr>
				<td>
					<%= genre.id %>
				</td>

				<td>
					<%= genre.name %>
				</td>
			</tr>
		<% end %>
	</tbody>
</table>